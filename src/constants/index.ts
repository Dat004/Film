import { dataAvatar } from './avatar';
import { dataFooter } from './footer';
import { dataList } from './list';
import { dataMenu, dataUserMenu } from './menu';

const data = {
  dataUserMenu,
  dataAvatar,
  dataFooter,
  dataMenu,
  dataList,
};

export { dataMenu, dataUserMenu, dataAvatar, dataFooter, dataList };
export default data;
