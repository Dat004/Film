/** Preview card / hover overlay timing & gesture thresholds. */
export const PREVIEW_HOVER_DELAY_MS = 900;
export const PREVIEW_HIDE_GRACE_MS = 220;
export const PREVIEW_TRAILER_DELAY_MS = 600;
export const PREVIEW_DRAG_THRESHOLD_PX = 10;
/** Mobile / coarse pointer max width for tap-to-preview. */
export const PREVIEW_TAP_MAX_WIDTH_PX = 768;
